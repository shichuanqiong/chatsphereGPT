{
  "rules": {
    ".read": "auth != null",
    ".write": false,

    "nicknameIndex": {
      "$slug": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.val() === auth.uid) || (data.exists() && data.val() === auth.uid && (!newData.exists() || newData.val() === auth.uid)))"
      }
    },

    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth.uid === $uid",
        ".validate": "newData.hasChildren(['state', 'lastSeen'])"
      }
    },

    "profiles": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['uid']) && newData.child('uid').val() === $uid"
      }
    },

    "profilesStats": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "roles": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },

    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && (newData.child('ownerId').val() === auth.uid || data.child('ownerId').val() === auth.uid)",
        ".validate": "newData.child('ownerId').exists()",
        "invites": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && (root.child('rooms').child($roomId).child('ownerId').val() === auth.uid || auth.uid === $uid)"
          }
        },
        "bans": {
          "$uid": {
            ".read": "auth != null",
            ".write": "auth != null && root.child('rooms').child($roomId).child('ownerId').val() === auth.uid"
          }
        }
      }
    },

    "roomMembers": {
      "$roomId": {
        "$uid": {
          ".read": "auth != null",
          ".write": "auth != null && (auth.uid === $uid || root.child('rooms').child($roomId).child('ownerId').val() === auth.uid)"
        }
      }
    },

    "roomsMeta": {
      "$uid": {
        "$roomId": {
          ".read": "auth != null && auth.uid === $uid",
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    },

    "messages": {
      "$roomId": {
        "$msgId": {
          ".read": "auth != null",
          ".write": "auth != null && newData.exists() && ((!data.exists() && newData.child('authorId').val() === auth.uid) || (data.exists() && data.child('authorId').val() === auth.uid)) && newData.child('content').isString()",
          ".validate": "newData.hasChildren(['authorId', 'content', 'createdAt'])"
        }
      }
    },

    "dmMessages": {
      "$threadId": {
        ".read": "auth != null && (root.child('dmThreads').child(auth.uid).child($threadId).exists() || $threadId.contains(auth.uid))",
        ".write": "auth != null && (root.child('dmThreads').child(auth.uid).child($threadId).exists() || $threadId.contains(auth.uid)) && newData.exists() && newData.hasChildren(['authorId', 'content', 'createdAt']) && newData.child('authorId').val() === auth.uid && newData.child('content').isString()"
      }
    },

    "dmThreads": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && (auth.uid === $uid || !data.exists())"
      }
    },

    "inbox": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && (auth.uid === $uid || !data.exists())"
      }
    },

    "announcements": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('profiles').child(auth.uid).child('isAdmin').val() === true"
    },

    "friends": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "blocks": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        "$peerUid": {
          ".read": "auth != null && auth.uid === $uid",
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    },

    "mutes": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },

    "ads": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('profiles').child(auth.uid).child('isAdmin').val() === true"
    },

    "posts": {
      "$postId": {
        ".read": "auth != null",
        ".write": "auth != null && newData.exists() && ((!data.exists() && newData.child('authorId').val() === auth.uid) || (data.exists() && data.child('authorId').val() === auth.uid))",
        ".validate": "newData.hasChildren(['authorId', 'content'])"
      }
    },

    "rateLimits": {
      "$roomId": {
        "$uid": {
          ".read": "auth != null && auth.uid === $uid",
          ".write": "auth != null && auth.uid === $uid"
        }
      }
    }
  }
}
