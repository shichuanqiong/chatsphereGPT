import { useEffect, useState } from 'react';
export default function Section({ id, title, children }:{ id:string, title:string, children:any }){const key=`sec:${id}:collapsed`;const [collapsed,setCollapsed]=useState(false);useEffect(()=>{setCollapsed(localStorage.getItem(key)==='1')},[]);const toggle=()=>{const v=!collapsed;setCollapsed(v);localStorage.setItem(key,v?'1':'0')};return(<div className='mb-3'><div className='flex items-center justify-between px-2 text-white/70 text-sm'><span>{title}</span><button onClick={toggle} className='text-xs'>{collapsed?'Expand':'Collapse'}</button></div>{!collapsed&&<div className='mt-2'>{children}</div>}</div>)}
